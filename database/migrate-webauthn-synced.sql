-- ============================================================
-- মাইগ্রেশন: পাসকি "সিঙ্ক হয় কিনা" চিহ্নিত করা
--
-- কেন: গুগল পাসওয়ার্ড ম্যানেজার (Android/Chrome) ও Apple iCloud Keychain যে পাসকি বানায়
-- সেটা ফোনের হার্ডওয়্যারে আটকে থাকে না — **গুগল/Apple অ্যাকাউন্টে সিঙ্ক হয়**। ফলে একই
-- অ্যাকাউন্টে লগইন করা অন্য ডিভাইসেও ঐ পাসকি কাজ করে (WebAuthn স্ট্যান্ডার্ডে এটাই স্বাভাবিক,
-- "multi-device credential"/Backup-Eligible)। রেজিস্ট্রেশনের সময় authData-র BE বিট (0x08)
-- দেখে জানা যায়, তাই সেটা সেভ করে নিরাপত্তা পেজে দেখানো হয় — অ্যাডমিন যেন অবাক না হন।
--
-- NULL = পুরনো এন্ট্রি (এই আপডেটের আগে যোগ করা, জানা নেই)।
-- non-destructive, idempotent (MariaDB)। লাইভ ও লোকাল দুই জায়গায় চালাতে হবে।
-- ============================================================

ALTER TABLE admin_webauthn_credentials
    ADD COLUMN IF NOT EXISTS is_synced TINYINT(1) DEFAULT NULL AFTER device_name;
