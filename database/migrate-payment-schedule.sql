-- ============================================================
-- মাইগ্রেশন: কোর্স-ব্যাচে পেমেন্ট সময়সূচি (কখন কত টাকা দিতে হবে) — অ্যাডমিন কোর্সভেদে একবার লেখেন।
-- রেজিস্ট্রেশন সফল কার্ডে (register-thanks) দেখায়। শুধু ডিসপ্লে — আয়/কালেকশন হিসাব ছোঁয় না।
-- non-destructive, idempotent। বাংলা ডিফল্ট লেখা নেই (mojibake এড়াতে)।
-- ============================================================
ALTER TABLE course_batches
    ADD COLUMN IF NOT EXISTS payment_schedule TEXT AFTER secondary_fee;
